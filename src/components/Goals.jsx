import { useState } from 'react';
export default function Goals({ state, setState }) { const [goal,setGoal]=useState(''); return <div><div className="inline"><input placeholder="Goal" value={goal} onChange={e=>setGoal(e.target.value)}/><button onClick={()=>{ if(goal){setState({...state,goals:[...state.goals,{id:crypto.randomUUID(),goal,done:false}]}); setGoal('');}}}>Add goal</button></div>{state.goals.map(g=><label className="check" key={g.id}><input type="checkbox" checked={g.done} onChange={()=>setState({...state,goals:state.goals.map(x=>x.id===g.id?{...x,done:!x.done}:x)})}/>{g.goal}</label>)}<textarea placeholder="Founder notes" value={state.notes} onChange={e=>setState({...state,notes:e.target.value})}/></div>; }
