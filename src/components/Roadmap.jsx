export default function Roadmap({ path }) { const steps = ['Validate the offer','Build proof sample','Contact 25 warm leads','Book first paid customer','Deliver and collect testimonial','Improve pricing','Create repeatable weekly system']; return <ol className="roadmap">{steps.map((s,i)=><li key={s}><strong>Days {i*4+1}-{Math.min(30,i*4+4)}</strong><span>{s} for {path?.title || 'your path'}</span></li>)}</ol>; }
